{
  "name": "tcho-burguer-functions",
  "description": "Cloud Functions do Tcho Burguer — CRM/Marketing (automações + WhatsApp oficial)",
  "engines": { "node": "20" },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
